add(x) : float or int
* Arguments:
  x - Array of floats or ints.
* Returns:
  Sum of all numbers in x.

----

subtract(x) : float or int
* Arguments:
  x - Array of floats or ints.
* Returns:
  Difference of all numbers in x.

----

multiply(x) : float or int
* Arguments:
  x - Array of floats or ints.
* Returns:
  Product of all numbers in x.

----

divide(x) : float
* Arguments:
  x - Array of floats or ints.
* Returns:
  Quotient of all numbers in x.

----

modulus(x) : int
* Arguments:
  x - Array of floats or ints.
* Returns:
  Modulus of all numbers in x.

----

factorial(x) : int
* Arguments:
  x - int
* Returns:
  Factorial of x.

----

range2(x) : int
* Arguments:
  x - Array of ints.
* Returns:
  Range of all numbers in x; the highest value subtracted from the lowest value.

----

isInRange(x, y, z) : bool
* Arguments:
  x - float or int
  y - float or int
  z - float or int
* Returns:
  true if x is greater than or equal to y and less than or equal to z, and false otherwise.

----

isInRange2(x, y, z) : bool
* Arguments:
  x - float or int
  y - float or int
  z - float or int
* Returns:
  true if x is greater than y and less than z, and false otherwise.

----

sign(x) : int
* Arguments:
  x - float or int
* Returns:
  0 if x is zero.
  1 if x is positive.
  -1 if x is negative.

----

crt(x) : float
* Arguments:
  x - float or int
* Returns:
  Cube root of x.

----

rt(x, y) : float
* Arguments:
  x - float or int
  y - float
* Returns:
  y root of x.

----

isEven(x) : bool
* Arguments:
  x - int
* Returns:
  true if x is even, and false otherwise.

----

isOdd(x) : bool
* Arguments:
  x - int
* Returns:
  true if x is odd, and false otherwise.

----

isPrime(x) : bool
* Arguments:
  x - int
* Returns:
  true if x is prime, and false otherwise.

----

isInteger(x) : bool
* Arguments:
  x - float
* Returns:
  true if x can be converted to an int exactly, and false otherwise.

----

isNatural(x) : bool
* Arguments:
  x - int
* Returns:
  true if x is natural, and false otherwise.

----

quadEquation(a, b, c) : seq[float, float]
* Arguments:
  a - float
  b - float
  c - float
* Returns
  Sequence containing the results of the quadratic equation.
* Note:
  This only finds the solutions, it does not check if they work.

----

pythagorean(a, b, c) : float
* Arguments:
  a - float
  b - float
  c - float
  These are used as the values in a^2 + b^2 = c^2.
* Returns:
  One of the arguments must be 0.0. This value will be returned, calculated from the other two.

----

gcd(x, y) : int
* Arguments:
  x - int
  y - int
* Returns:
  Greatest common divisor of x and y.

----

lcm(x, y) : float
* Arguments:
  x - int
  y - int
* Returns:
  Least common multiple of x and y.

----

deg2rad(x) : float
* Arguments:
  x - float
* Returns:
  Radian equivalent of x degrees.

----

rad2deg(x) : float
* Arguments:
  x - float
* Returns:
  Degree equivalent of x radians.

----

dividesEvenly(x, y) : bool
* Arguments:
  x - int
  y - int
* Returns:
  true if x can be divided by y with no remainder, and false otherwise.

----

reverseFactorial(x) : int
* Arguments:
  x - int
* Returns:
  The number that has a factorial of x.

----

polygonAngles(n) : int
* Arguments:
  n - int
* Returns:
  Sum of the interior angles of a polygon with n sides.
