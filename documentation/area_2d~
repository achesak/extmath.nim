Math.areaRect(x, y)
* Arguments:
  x - Number
  y - Number
* Returns:
  Area of the rectangle with width x and height y.

----

Math.areaTri(b, h)
* Arguments:
  b - Number
  h - Number
* Returns:
  Area of the triangle with base b and height h.

----

Math.areaCircle(r)
* Arguments:
  r - Number
* Returns:
  Area of the circle with radius r.

----

Math.areaParallel(x, y)
* Arguments:
  x - Number
  y - Number
* Returns:
  Area of the parallelogram with width x and height y.

----

Math.areaTrap(b1, b2, h)
* Arguments:
  b1 - Number
  b2 - Number
  h - Number
* Returns:
  Area of the trapezoid with bases b1 and b2 and height h.

----

Math.areaRhom(d1, d2)
* Arguments:
  d1 - Number
  d2 - Number
* Returns:
  Area of the rhombus with diagonal lengths d1 and d2.

----

Math.areaEllipse(a, b)
* Arguments:
  a - Number
  b - Number
* Returns:
  Area of the ellipse with radii a and b.

----

Math.areaSector(a, r)
* Arguments:
  a - Number
  r - Number
* Returns:
  Area of the sector with arc length a and radius r.

----

Math.areaKite(x, y)
* Arguments:
  x - Number
  y - Number
* Returns:
  Area of the kite with diagonals x and y.

----

Math.areaKiteTrig(x, y, theta)
* Arguments:
  x - Number
  y - Number
  theta - Number
* Returns:
  Area of the kite with unequal sides x and y and included angle theta.
