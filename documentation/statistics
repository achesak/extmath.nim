mean(x) : float
* Arguments:
  x - Array of floats or ints.
* Returns:
  Mean (average) of the numbers in x.

----

weightedMean(x) : float
* Arguments:
  x - Array of arrays, each with two floats.
* Returns:
  Weighted mean, calculated from the first values of the arrays as the
  number and the second values as the percentage.

----

median(x) : float
* Arguments:
  x - Array of floats.
* Returns:
  Median of the numbers in x.

----

stdDev(x) : float
* Arguments:
  x - Array of floats or ints
* Returns:
  Standard deviation of the numbers in x.
